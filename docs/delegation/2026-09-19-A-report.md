# Task A report — stage-first catalog

Date: 2026-09-19. Worker: Cursor Grok 4.6 Fast. Scope: plan Task A only (S1/S2/S5 catalog/navigation), including review corrections. No commit, push, or deploy. `.claude/worktrees/gamification-phase-1` was not read, edited, removed, or staged.

## Executed

Stage-first navigation remains: homepage shows all 12 grades in 5/4/3 groups; grade 8 on the home page goes to `/stage/intermediate#grade-8`; that card on the stage page goes to `/grade/8`; science goes to the existing science index; nutrients remains the only publishable lesson. Upcoming cards render the exact status `في رحلتنا القادمة` and are `<article>` elements with no `href`. Stage routes are browsable without published content. Availability is derived from `registeredLessons(published)` in `src/content/catalog.ts`; a local PDF is not a registered lesson.

Review corrections in this pass:

1. Grade page copy is learner-facing. When science is published: «اختاري مادة وابدئي رحلة جديدة من الاكتشاف. العلوم متاحة الآن، وباقي المواد في رحلتنا القادمة.» When unpublished, only the first sentence. Image alts and grade metadata no longer mention Task C, temporary icons, or publication internals.
2. Quran 2027 cover inspected visually: **مادة القرآن الكريم**, الصف الثامن، الفصل الدراسي الأول، المرحلة المتوسطة، وزارة التربية. Added as upcoming subject `quran`. Not claimed as a complete 2026–2027 Ministry list.
3. Header has `المراحل الدراسية` → `/#grades`, active on `/stage/*` and grade-root `/grade/{n}`. `استكشفي العلوم` stays a shortcut, active only on science routes.
4. Other-worker worktree left untouched; no repository-recursive formatting.

## Gate outcomes (review pass)

| Command | Result |
| --- | --- |
| `npm.cmd test` | **54 pass / 0 fail** |
| `npm.cmd run build` | **success** (Next.js 16.3.5). Routes unchanged from first Task A pass: `ƒ /`, `ƒ /stage/[stage]`, `ƒ /grade/[grade]`, existing science/nutrients |
| `npx.cmd playwright test tests/browser/catalog-navigation.spec.ts tests/browser/deployed-improvements.spec.ts tests/browser/accessibility.spec.ts` | **10 passed / 0 failed** in 18.1s |

Publication toggles used only `.data/browser-qa.sqlite` via `/api/admin/publish`, with restore to `published: true` in `finally`. Real user publication settings were not changed. Escape/menu assertions in `deployed-improvements.spec.ts` were not loosened.

## Subject-list evidence

Recorded in `docs/CATALOG-SOURCES.md`. Displayed grade-8 subjects (8 cards; science is the only link when published):

| id | Title shown | Evidence |
| --- | --- | --- |
| arabic | اللغة العربية | Folder `GRADE-8/FIRST-TERM/ARABIC` and filenames |
| english | اللغة الإنجليزية | Folder `ENGLISH`; Pearl of Kuwait student-book PDFs. Cover text extract is English (`Student’s Book` / `Grade`), not an Arabic title |
| math | الرياضيات | `MATH STUDENT BOOK.pdf` first-page extract includes «الرياضيات» and «الصفّ الثامن» |
| science | العلوم | Folder `SCIENCE`, `علوم.pdf`, `docs/SCIENCE-SOURCE-MAP.md` |
| quran | القرآن الكريم | Visual cover of `ISLAMIC/كتاب ثامن قرآن ٢٠٢٧.pdf` (pdftoppm): **مادة القرآن الكريم**, الصف الثامن، الفصل الدراسي الأول، المرحلة المتوسطة. File lives under ISLAMIC but is a separate subject book |
| islamic | التربية الإسلامية | `كتاب ثامن إسلامية ٢٠٢٧.pdf` extract includes «التربية الإسلامية» |
| social-studies | الدراسات الاجتماعية | Folder `SOCIAL STUDIES` only. Local file is a 2025 solution booklet, not a 2026–2027 student cover |
| home-economics | الاقتصاد المنزلي | Visual cover of `ECONMIC/الاقنصاد المنزلي.pdf`: **الاقتصاد المنزلي**. Folder name is not used in the UI |

## Ministry completeness check (unverified)

- [elibrary.moe.edu.kw/StudentsLibrary](https://elibrary.moe.edu.kw/StudentsLibrary) did not expose a static 2026–2027 grade-8 subject list.
- Official 2016 intermediate plan also lists تكنولوجيا الاتصالات والمعلومات، التربية البدنية، التربية الفنية، التربية الموسيقية، دراسات عملية. Dated **2016م**. Those extras have no inspected 2026–2027 local books here and were **not** added.

The displayed list is locally evidenced, not a complete Ministry 2026–2027 set.

## Deviations and unverified work

- Science-link clicks in the journey test stay scoped to `#main-content` because header/footer also mention العلوم. Route contract is unchanged.
- Subject-card visuals remain temporary Lucide icons (science reuses `/images/science-discovery.png`). Provenance stays in docs, not in alts. Task C artwork is not complete.
- Grade-8 social studies has no inspected student-book cover.
- English Arabic title is from the folder/context, not an Arabic cover line.
- Hash-scroll to `#grade-8` uses `scroll-margin-top`; no new screenshot pass in this correction run.
- `next build` may load `.env.local`; this worker did not read, echo, or edit env or production `.data` databases.

## Spec/plan incompatibilities (not edited)

None that blocked S1/S2 routing. Completeness of the Ministry subject list remains honestly incomplete.

## Out of scope (as instructed)

Audio transport (Task B), generated textbook artwork (Task C), git add/commit/push/deploy, and the gamification worktree.
