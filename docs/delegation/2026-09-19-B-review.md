# Audio review follow-up

After your initial Task B run, resolve these independent code-review findings before acceptance:

1. `loadSegment().then(...)` currently executes `audio.pause()` when `!isLive(token)`. A stale resolved play promise from segment A must not pause segment B on the shared element. Return immediately for a stale token; only a live operation may apply its current paused intent. Review resume's promise path too. Add a deterministic regression that delays resolution of an older play promise, moves to a new segment, resolves the older promise, and proves the new segment stays playing. The existing delayed-network/pause test does not cover this case.
2. At1440px the lesson container is1184px wide (right margin128px), but a fixed248px dock at right16px overlaps136px of content. Reserve a desktop rail or otherwise lay out the hero, navigation AND lesson body so reading targets never run beneath the dock. At768/1024 use a compact lower dock if the rail would make content unusable. Test actual bounding rectangles of visible right-hand headings/targets vs dock at multiple scroll positions, not only a center quiz button. Mobile bottom padding must account for actual expanded height/safe-area; minimize should genuinely shrink width on mobile (base min-width rules are overridden by mobile .audio-dock width).
3. Replace learner-visible «لا يوجد طابور تشغيل نشط» with simple copy such as «اضغطي تشغيل لنبدأ الشرح». The UI should not expose queue terminology.
4. Label the starting state's pause control with its actual action «إيقاف مؤقت», while loading text remains a separate status. The main player's starting-state control must call pause, not restart the entire page. Give resume a clear accessible «إكمال» label consistently and scope/update corresponding existing test selectors without weakening assertions.

Keep the state architecture and grants, independent fixtures, no real audio generation. Run npm.cmd test, build, the full Task B browser selection after corrections. Report outcomes and any remaining issues. No commit/push/deploy; don't touch another worker's .claude worktree. Preserve A and the C brief.
