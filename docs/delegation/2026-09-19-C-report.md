# Task C report — curriculum visuals

Date: 2026-09-19. Worker: Cursor Grok 4.6. Scope: plan Task C / spec S3 and S5 visual items, with the owner's addendum that **no images are generated in this lane**. No git add/commit/push/deploy. `.claude/worktrees/` was not read, edited, or removed. Pre-existing untracked screenshots `docs/deployed-home-before.png`, `docs/deployed-lesson-mobile-before.png`, and `docs/improved-search-mobile.png` were not touched.

## Asset generation: pending

Task C image generation is **not complete**. There is no image-generation pass against the local textbook pages in this worker, by standing instruction (Codex Desktop will generate later). No Image API, download, or fabricated PNG was used.

All fourteen needed new images are registered in `src/content/image-slots.ts` with `ready: false`. The renderer `src/components/ui/image-slot.tsx` shows an on-brand aspect-ratio placeholder (palette tokens, topic icon, no rasterized text, no broken `<img>`). Flipping a slot to ready after dropping the file at its path is the only connect step.

Existing generated files were **not** relabeled as Task C deliverables. The nutrients lesson page still uses `/images/nutrients-plate.png`. The homepage science scene and the live العلوم subject card still use `/images/science-discovery.png`.

## Image slots created (all pending)

Curriculum (`public/images/curriculum/<id>.png`):

1. `nutrients` — 800×800 — lesson card المغذّيات — ref `علوم-24.jpg`
2. `balanced-diet` — 800×800 — lesson card النظام الغذائي المتوازن — ref `علوم-29.jpg`
3. `digestive-structure` — 800×800 — lesson card تركيب الجهاز الهضمي — ref `علوم-38.jpg`
4. `digestive-accessories` — 800×800 — lesson card ملحقات الجهاز الهضمي — ref `علوم-43.jpg`
5. `digestion` — 800×800 — lesson card عملية الهضم — ref `علوم-48.jpg`
6. `life-header` — 1600×400 — left pane of the علوم الحياة header — refs `علوم-29.jpg`, `علوم-38.jpg`, `علوم-43.jpg`

Subject cards (`public/images/subjects/<id>.png`), 768×768:

7. `arabic`
8. `english`
9. `math`
10. `science` (reserved; live card keeps `science-discovery.png` until this slot is ready)
11. `quran`
12. `islamic`
13. `social-studies`
14. `home-economics`

Prompts, alts, usage, and reference paths: `docs/delegation/2026-09-19-image-requests.md` and `src/content/image-slots.ts`.

Textbook pages 24/29/38/43/48 were opened locally and match the orchestrator's 2026-09-19 verification. Original textbook files were not edited.

## Code work executed

- Visual lookup keyed by lesson id (`lessonVisual` / `lifeHeaderVisual` / `subjectVisual`).
- Life unit header: live HTML «علوم الحياة» and description on the **right**; image slot on the **left**. Other units keep the previous icon header. Section id `life` and h2 hierarchy unchanged.
- Lesson cards: reserved square beside the title. Five life-science lessons use ImageSlot; the other fourteen use topic Lucide icons in the same square. HTML titles stay outside images. 19 catalog lessons, nutrients still the only `available` lesson, search normalization unchanged.
- Grade-8 subject cards: seven pending slots; العلوم keeps the existing photograph.
- Catalog intro (`.catalog-heading p`) remains visible at 390px. The generic rule that hides `.section-heading > p` no longer hides catalog availability copy.
- Tasteful enter/hover motion, `transform`/`opacity` only, gated by `prefers-reduced-motion`.
- next/image path is wired for `ready: true` with explicit width/height, sizes, and reserved boxes.
- axe routes extended to `/stage/primary`, `/stage/intermediate`, `/stage/secondary`, and `/grade/8`.

## Distinctions

| Kind | What |
| --- | --- |
| Executed | Slot registry, placeholder renderer, catalog/header layout, CSS, tests, this report |
| Existing (not Task C) | `public/images/science-discovery.png`, `nutrients-plate.png`, `food-oats.png`, `food-lentils.png`, `food-oil.png`, `food-milk.png` as already documented in `docs/GENERATED-IMAGES.md` |
| Pending | All fourteen slots above; S5 item 6 (reference-generated local assets) is unmet until Codex Desktop writes the files and `ready` is flipped |
| Unverified | Pixel-perfect anatomy review against the textbook after generation; Ministry-complete subject list (still Task A open item) |

## Gates

| Command | Result |
| --- | --- |
| `npm.cmd test` | **59 pass / 0 fail** (5 new slot-registry tests) |
| `npm.cmd run build` | **success** (Next.js 16.3.5) |
| `E2E_PORT=3019 npx.cmd playwright test tests/browser/catalog-navigation.spec.ts tests/browser/deployed-improvements.spec.ts tests/browser/accessibility.spec.ts tests/browser/learning.spec.ts` | **16 passed / 0 failed** in 1.0m |

Port 3000 was not touched. Fixture DB `.data/browser-qa.sqlite` only. No commit, push, or deploy.
