# مدارك — تسليم العمل لاستكماله

**Snapshot:** 2026-09-19, Asia/Kuwait. Prepared because the user requested a local handover before the current usage quota runs out. This file is a checkpoint, not a claim that all work is finished.

## Start here

Workspace: **E:/HANA-WALEED**, PowerShell, branch **main**.
Latest reviewed local commit: **2e57735** — `Add stage-first curriculum catalog and implementation specification`.
No push or deployment was performed in this work session. Production remains independent of these local changes.

**A Cursor audio-review run is still pending at this snapshot. Inspect its result before editing shared files or dispatching another writer. Do not restart the entire project.**

## User's latest decisions and scope

- User asked to create the specification and implementation plan, then delegate implementation to **Cursor Grok 4.6 Fast**. Use Cursor directly, not Claude as an intermediate implementer.
- Homepage is grouped into **المرحلة الابتدائية، المرحلة المتوسطة، المرحلة الثانوية**. Display grades1–12, grouped5/4/3. Upcoming grade/subject cards say exactly **في رحلتنا القادمة** and are not links.
- Explicit confirmed navigation: homepage grade8 card → **/stage/intermediate#grade-8** → grade8 card → **/grade/8** subjects → **/grade/8/science** → nutrients lesson. Do not simplify this to direct home→subjects or home→science.
- Only grade8 science, first term2026–27, with nutrients implemented/published. Other textbooks on disk do not mean their lessons are published.
- Audio: **previous while paused selects the prior narration segment at zero and STAYS PAUSED until إكمال**. Replay current is separate from restart the whole queue. One audio provider/engine, shared top player and persistent right-hand dock.
- Images: realistic generated imagery using the local textbook as reference; five lesson thumbnails plus علوم الحياة wide header, imagery on LEFT and readable HTML title space on RIGHT. Every subject card should eventually have suitable imagery. Do not claim existing photos/icons are newly generated reference-based art.
- Palette: #ffbe0b / #fb5607 / #ff006e / #8338ec / #3a86ff. Arabic RTL, local font, reduced-motion support, responsive and accessible.
- Broader gamification ideas (marketplace/streak/leaderboard/dashboard additions) are not this task. Another worker has a gamification worktree: preserve it.

## Saved specification, plan and delegation briefs

1. `docs/superpowers/specs/2026-09-19-stages-catalog-audio.md` — approved requirements and S1–S5 acceptance criteria.
2. `docs/superpowers/plans/2026-09-19-stages-catalog-audio.md` — file map, implementation sequence, asset directions and gates.
3. `docs/delegation/2026-09-19-STATUS.md` — task progress (check current state against actual results).
4. `docs/delegation/2026-09-19-A-catalog.md` and `2026-09-19-A-review.md` — completed catalog brief and review corrections.
5. `docs/delegation/2026-09-19-B-audio.md` and **`2026-09-19-B-review.md`** — audio brief and currently running review fixes.
6. `docs/delegation/2026-09-19-C-visuals.md` — prepared but **NOT DISPATCHED**. Includes expanded accessibility and visual bounds checks.

## A — Completed and independently reviewed

Implemented stage-first catalog, all12grade cards, three stage pages, grade8 subjects page, publication-derived availability, breadcrumbs, header stages link, canonical metadata and sitemap updates.

Important files:
- `src/content/catalog.ts`: stages, subject definitions, registered lesson and availability helpers.
- `src/components/grade-cards.tsx`, `stage-page.tsx`, `grade-subjects.tsx`.
- `src/app/stage/[stage]/page.tsx`, `src/app/grade/[grade]/page.tsx`.
- Updates to home/page/shell/curriculum/lesson breadcrumbs/globals/sitemap.
- `tests/catalog.test.mjs`, `tests/browser/catalog-navigation.spec.ts`.

Reviewed corrections: removed implementation language from student UI and image alt text; added Quran after inspecting current local cover; distinguished stages-active navigation from science-active navigation.

**Independent orchestrator evidence:** npm.cmd test54passed; npm.cmd run build succeeded;10browser tests passed (catalog-navigation + deployed-improvements + accessibility). Catalog test covers publication toggling using the fixture database, restored after test. Current subject page shows8cards and one available science card. Playwright independently checked12homegrades,8subjects and no horizontal overflow at390px. Screenshots: `docs/delegation/catalog-desktop-review.png`, `docs/delegation/subjects-mobile-review.png`.

**Subject-list limitation:** eight locally evidenced subjects: العربية، الإنجليزية، الرياضيات، العلوم، القرآن الكريم، التربية الإسلامية، الدراسات الاجتماعية، الاقتصاد المنزلي. ECONMIC cover confirms الاقتصاد المنزلي. `docs/CATALOG-SOURCES.md` details evidence. A Ministry2016 plan lists additional subjects, but a complete current2026–27 list was NOT verified. Do not claim every current Ministry subject is already confirmed. Subject imagery is still temporary icons/one existing generated science image.

Task A sessions/artifacts:
- Cursor session `e8406e49-4cab-48ca-b6ab-b3b2fdb96fa8`.
- `C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-catalog/result.json`.
- Review run in sibling directory `madarek-20260919-catalog-review`.
- Actual model init confirmed `Cursor Grok 4.6 High Fast`.

## B — Implemented draft, review fixes STILL RUNNING

Initial Cursor run reported54unit tests/build/31browser tests passed. These are implementer claims; final independent acceptance is NOT complete. Initial report is `docs/delegation/2026-09-19-B-report.md`, which may be stale until the resumed run updates it.

Dirty/uncommitted B files at snapshot:
- `src/components/audio/audio-provider.tsx`: segmentIndex/count, canPrevious/Next, replaySegment, intent-aware manual navigation, event and promise handling.
- `src/components/audio/player-bar.tsx`: shared transport/capabilities, replay-current control, labels.
- New `src/components/audio/floating-audio-dock.tsx`: right dock/mobile controls, minimize/expand, status.
- `src/components/lesson.tsx`: dock in same provider.
- `src/app/globals.css`: dock/rail/mobile clearance.
- New `tests/browser/audio-dock.spec.ts` and scoped selectors in `audio-player.spec.ts`.

Independent review found and sent these issues back to Cursor:
1. A stale play promise was pausing the NEW segment on the shared audio element. Stale tokens must return without mutating it; add a delayed-promise resolution regression.
2. Desktop248px dock at right16px overlapped the1184px lesson container at1440px. Reserve a rail for hero/nav/body and use compact dock below1200px; test real bounds at multiple scroll points. Mobile clearance should reflect measured dock height and minimized width should truly shrink.
3. Replace queue jargon in student text.
4. Starting-state pause button must pause instead of restart; resume accessible label should be إكمال consistently.

Some fixes already appeared in the working tree (stale-token early return, desktop rail, new labels). **Do not infer completion from partial edits.**

### Inspect the running review first

Cursor session: **ea41dd45-f847-46b2-be8f-3e21a1456133**.
Model ID: **cursor-grok-4.6-high-fast** (verified by live `cursor-agent models`).
Initial run: `C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-audio/result.json`.
CURRENT review directory: **C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-audio-review**.
At snapshot, review `result.json` did not exist. Relay process runs through unified-exec session **55476** in the original Codex task. A wait cell **187** may still be yielding that session; tool handles might not survive a different conversation. Disk result/process state is the durable check.

```powershell
$reviewPath = 'C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-audio-review/result.json'
if (Test-Path -LiteralPath $reviewPath) {
  $review = Get-Content -LiteralPath $reviewPath -Raw | ConvertFrom-Json
  $review | Select-Object status, sessionId, resolvedModel, exitCode, finishedAt
  $review.finalMessage
} else {
  Get-Item 'C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-audio-review/events.jsonl' |
    Select-Object Length, LastWriteTime
}
git status --short
git diff --stat
```

Do not dump entire events.jsonl (large internal event stream); prefer result.json/finalMessage and scoped tool-result inspection. If the process vanished without result, preserve edits, inspect status/diff and report interrupted state; do not reflexively reset.

After review completion, read the B diff and actual tests; rerun:

```powershell
npm.cmd test
npm.cmd run build
npx.cmd playwright test tests/browser/audio-dock.spec.ts tests/browser/audio-player.spec.ts tests/browser/audio-e2e.spec.ts tests/browser/audio-targets.spec.ts tests/browser/narration-targets.spec.ts tests/browser/lesson-feedback.spec.ts tests/browser/accessibility.spec.ts
```

Visual acceptance also needs360/390/768/1024/1440 widths, no dock/readable-text collision, no blocked quiz buttons, one audio engine, and actual paused/source/index checks. Mock WAV/MP3 tests do not prove production narration is available. Do not generate or approve real audio in this scope.

## C — Prepared, not dispatched; images still missing

Next run, only after B has finished writing shared CSS/lesson files:

```powershell
node 'C:/Users/waleed Mohamd/.agents/skills/cursor-delegate/scripts/relay.mjs' --brief docs/delegation/2026-09-19-C-visuals.md --cd E:/HANA-WALEED --model cursor-grok-4.6-high-fast --timeout 1h --out-dir 'C:/Users/waleed Mohamd/AppData/Local/Temp/madarek-20260919-visuals'
```

Use the cursor-delegate skill: inspect current CLI status/model availability, dispatch from PowerShell, wait for a result, independently review/tests, then orchestrator commits. Cursor must not commit/push/deploy. Preserve the existing user-approved lane. If more B fixes are needed, resume the explicit B session with a small delta brief; never `--resume-last` when multiple sessions exist.

Reference images were visually inspected by the orchestrator:
- `GRADE-8/FIRST-TERM/SCIENCE/علوم/علوم-24.jpg` — المغذّيات.
- `علوم-29.jpg` — النظام الغذائي المتوازن.
- `علوم-38.jpg` — تركيب الجهاز الهضمي.
- `علوم-43.jpg` — ملحقات الجهاز الهضمي; includes salivary glands, liver/gallbladder/pancreas.
- `علوم-48.jpg` — عملية الهضم.
Printed numbers and lesson names match those filenames. These are references, NOT generated deliverables. Use29/38/43 together for the life header if appropriate.

Desired new local assets under `public/images/curriculum/`: nutrients.png, balanced-diet.png, digestive-structure.png, digestive-accessories.png, digestion.png, life-header.png, plus suitable subject visuals. Preserve original textbook images. Actual image generation has **NOT** happened during this session.

The C brief explicitly requires honest capability handling: if Cursor cannot access a suitable image-generation tool, it should finish independent layout work and return exact reference/prompt/output requests in `docs/delegation/2026-09-19-image-requests.md`, marking assets incomplete. The orchestrator's available `image_gen.imagegen` tool can supply missing images afterward; don't fabricate generation or silently substitute stock photos.

Image tool notes if needed: inspect local references before editing/generating; use referenced_image_paths, never combine it with num_last_images_to_include. Built-in tool doesn't expose a selectable “GPT2.5” model—report actual capability. Its return may contain a huge data URL: **do not print JSON/text of the whole result**. Print only output_hint or display generatedImage(result); copy actual saved PNG into public and record prompt/reference provenance in docs/GENERATED-IMAGES.md. Do not use Python to create fake generated illustrations.

C also needs axe coverage for the new stage/grade pages (existing accessibility spec currently focuses on old routes), bounded-heading checks (overflow:hidden can mask clipping), readable catalog intro on mobile, preserved Arabic search and19lesson count/one active lesson.

## Repository/runtime boundaries

- Node24, Next16.3.5, React19.3, FramerMotion13.4; scripts in package.json. AGENTS.md requires installed Next docs before framework changes.
- User-invoked skills: ui-styling/ui-ux-pro-max in `C:/Users/waleed Mohamd/.agents/skills/`. Cursor relay same root `/cursor-delegate/scripts/relay.mjs`.
- Do not read/echo/commit `.env.local` or secrets. A pasted key in earlier chat is not to be reproduced. No real database/audio library changes.
- Browser tests use `.data/browser-qa.sqlite`, `.data/audio-e2e`, fixture narration and port3001; E2E_PORT can override. Build before Playwright (`next start`). Do not kill unrelated port3000 server. Orchestrator's temporary port3002 server session84726 was stopped after screenshots.
- **Other worker's `.claude/worktrees/gamification-phase-1` is untracked at root and protected/out of scope. Never git add . / git add -A / reset --hard / clean.** Use explicit owned paths when committing. No repo-wide formatting over nested worktrees.
- Preserve pre-existing untracked docs/deployed-home-before.png, docs/deployed-lesson-mobile-before.png, docs/improved-search-mobile.png.
- User's deployed URL: https://madarek.cc/ ; GitHub lolotam/Madarek. Pushing main can auto-deploy, so the current work has intentionally remained local. Don't claim deployment without live verification.

## Final closeout checklist

- [x] Save specification and implementation plan.
- [x] Delegate A to requested Cursor model; review corrections and independently verify; local commit2e57735.
- [ ] Wait for B review result; independently validate transport race/layout fixes and final test results; commit owned files only after acceptance.
- [ ] Dispatch C; review image capability and complete actual reference-based assets/integration; no false completion if assets are missing.
- [ ] Verify full navigation/search/audio/a11y behavior and screenshots across sizes.
- [ ] Update STATUS and reports with actual final evidence and outstanding subject-list uncertainty.
- [ ] Report local vs deployed state clearly to the user. No deployment has been done in this session.

Suggested continuation request: «اقرأ docs/HANDOVER-2026-09-19.md، افحص نتيجة مراجعة Cursor الجارية أولًا، ثم استكمل المهام بنفس Cursor Grok 4.6 Fast مع مراجعة مستقلة، وحافظ على شغل gamification المنفصل.»
